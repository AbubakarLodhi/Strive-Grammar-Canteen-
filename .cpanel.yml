---
deployment:
  tasks:
    # Runs after you click "Deploy HEAD Commit" in cPanel → Git Version Control.
    # Edit deploy/config on the server (copy from deploy/config.example) with your paths.
    - /bin/bash scripts/cpanel-deploy.sh
